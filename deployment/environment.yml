name: model-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - azureml-defaults
    - inference-schema
    - pandas
    - numpy
    - scikit-learn
    - joblib
    - azure-storage-blob